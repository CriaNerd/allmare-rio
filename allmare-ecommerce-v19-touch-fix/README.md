# Allmare Ecommerce V19 — Touch Fix

Correção específica para celulares/tablets que reportam viewport lógico acima de 900px. A coleção é tornada visível por detecção de toque (`pointer: coarse` / `maxTouchPoints`), sem depender de IntersectionObserver.

Render: `npm install` / `npm start`.
